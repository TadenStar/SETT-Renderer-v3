"""Скрипты, которые запускаются ВНУТРИ Blender через ``--python``.

Из приложения их не импортировать: здесь есть ``import bpy``, которого
снаружи Blender не существует. Путь к ним отдаёт ``core.blender_process.script_path``.
"""
